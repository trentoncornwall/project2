<nav>
  <div class="nav-wrapper #0097a7 cyan darken-2">
    <a href="#" class="brand-logo center">ITILITY</a>
    {{!-- <ul id="nav-mobile" class="left hide-on-med-and-down">
      <li><a href="/">Login</a></li>
      <li><a href="/home">home</a></li>
      <i class="material-icons">account_circle</i>
      <a id="signout" href="#" onclick="signOut();" color="black">Sign out</a>
    </ul> --}}
  </div>
</nav>

<div class="container">
    <div class="row">
        <div class="col s12 m6">
            <div class="card blue-grey darken-1">
                <div class="card-content white-text">
                    <span class="card-title">Log In</span>
                </div>
                <div class="card-action">

                    <div class="row">
                        <div class="g-signin2" data-onsuccess="onSignIn"></div>
                        <script>
                            function onSignIn(googleUser) {
                                console.log('ehre')
                                var profile = googleUser.getBasicProfile();
                                console.log('ID: ' + profile.getId()); // Do not send to your backend! Use an ID token instead.
                                console.log('Name: ' + profile.getName());
                                console.log('Image URL: ' + profile.getImageUrl());
                                console.log('Email: ' + profile.getEmail()); // This is null if the 'email' scope is not
                                const email = profile.getEmail();
                                window.location.replace(`/user/${email}`)
                            }
                        </script>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>